include = "../../configs/proteus.provider.example.toml"

[profile]
name = "dev-slim"

[modules]
# Slim profile for dogfooding Proteus itself. Keep this focused on the active
# coding-agent path; parked/research capabilities stay out of the hot surface.
workflow = "coding.single_loop"
search = "rg"
context = "repo_aware"
policy = "ask_write"
patch = "direct"
compactor = "codex"
renderer = "statusline"

[components.reference-workflow]
command = "proteus-reference-worker"

[components.reference-workflow.exports.workflow."coding.single_loop"]

[components.reference-context]
command = "proteus-reference-worker"

[components.reference-context.exports.context.repo_aware]

[components.reference-capabilities]
command = "proteus-reference-worker"

[components.reference-capabilities.exports.search.rg]

[components.reference-capabilities.exports.policy.ask_write]

[components.reference-capabilities.exports.patch.direct]

[components.reference-capabilities.exports.compactor.codex]

[components.reference-capabilities.exports.renderer.statusline]

[components.reference-capabilities.exports.tool."reference.tools"]

[tools]
enabled = ["search", "read_file", "list_dir", "find_files", "grep", "git_status", "git_diff", "request_user_input", "apply_patch", "shell"]

[permissions]
mode = "normal"

[module_config.policy.ask_write]
allow = ["search", "read_file", "list_dir", "find_files", "grep", "git_status", "git_diff", "request_user_input"]
ask_before = ["apply_patch", "shell"]

[module_config.compactor.codex]
trigger_fraction = 0.8

[module_config.context.repo_aware]
providers = ["project_instructions", "manifest", "git_status", "repo_tree", "search"]
max_context_bytes = 25000
max_bytes_per_file = 5000
max_search_results = 15
repo_tree_max_entries = 160
repo_tree_max_depth = 3
repo_tree_skip_entries = [
  ".git",
  "target",
  "node_modules",
  ".proteus",
  "sessions",
  "dist",
  "build",
  "examples/source",
  "examples/research",
  "clients/web/target",
  "docs/research",
  "docs/spec.md",
]
project_instruction_files = ["AGENTS.override.md", "AGENTS.md", "CLAUDE.md", ".cursorrules"]
manifest_files = ["Cargo.toml", "package.json", "pyproject.toml", "go.mod", "pom.xml", "build.gradle", "composer.json"]

[app_server]
approval_timeout_ms = 0

[runtime]
model_timeout_ms = 10800000
context_timeout_ms = 30000
workflow_timeout_ms = 14400000

[event_log]
path = ".proteus/events.jsonl"
