name = "claude-pack"
version = "0.1.0"
description = "Claude-Code-like behavior pack: phased workflow, tool exposure, and Claude-style tool aliases"
capabilities = ["workflow", "tool_exposure", "tool"]
