name = "shell-tool"
version = "0.1.0"
description = "Shell tool plugin: runs arbitrary shell commands via sh -lc. Opt-in because it is RunsCommands-safety."
author = "proteus-core examples"
tags = ["tool", "shell", "command"]
library = "libshell_tool.so"
requires_proteus_contracts = "0.1"
