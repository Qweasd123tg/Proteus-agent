name = "shell-tool"
version = "0.1.0"
description = "Shell tool plugin: trusted direct one-shot and interactive PTY execution (sh -lc), with optional PROTEUS_SHELL_SANDBOX=1 workspace sandbox."
author = "proteus-core examples"
tags = ["tool", "shell", "command", "interactive", "session"]
library = "libshell_tool.so"
requires_proteus_contracts = "0.1"
