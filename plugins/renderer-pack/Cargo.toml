[package]
name = "renderer-pack"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
plugin-entrypoint = []

[dependencies]
abi_stable = "0.11"
agent-contracts = { path = "../../crates/agent-contracts" }
anyhow = "1"
serde_json = "1"
