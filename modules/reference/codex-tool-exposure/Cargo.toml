[package]
name = "codex-tool-exposure"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
plugin-entrypoint = []

[dependencies]
abi_stable = "0.11"
proteus-contracts = { path = "../../../crates/proteus-contracts" }
serde_json = "1"
