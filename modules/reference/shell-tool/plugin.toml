name = "shell-tool"
version = "0.1.0"
description = "Shell tool plugin: one-shot 'shell' plus interactive PTY sessions 'exec_command'/'write_stdin' (sh -lc). Opt-in because it is RunsCommands-safety."
author = "proteus-core examples"
tags = ["tool", "shell", "command", "interactive", "session"]
library = "libshell_tool.so"
requires_proteus_contracts = "0.1"
